PROGRAM testeproc1;
    VAR
        x, y, z :INTEGER;

    PROCEDURE p;
    VAR
        a :INTEGER;
    BEGIN (* teste *)
        READLN(a);
        IF a=x THEN
            z:=z+x
        ELSE BEGIN
            z:=z-x;
            CALL p;
        END;
    END;
    BEGIN
        z:=0;
        READLN(x,y);
        IF x>y THEN
            CALL p
        ELSE
            z:=z+x+y;
        WRITELN(z);
    END
.